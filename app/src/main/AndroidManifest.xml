<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marcus.galgeleg">
    tools:replace="android:value"
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.marcus.galgeleg.Singleton"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"/>
        <activity android:name=".Hovedmenu_akt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Spil_akt" />
        <activity android:name=".Hjaelp_akt" />
        <activity android:name=".Vundet_akt" />
        <activity android:name=".Tabt_akt"></activity>
    </application>

</manifest>